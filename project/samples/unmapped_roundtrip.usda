#usda 1.0
(
    defaultPrim = "Root"
    upAxis = "Y"
)

def Xform "Root"
{
    custom string debugLabel = "car"
    custom float3 debugDirection = (1, 2, 3)
    custom int[] debugIds = [1, 3, 5]
    custom rel debugTarget = </Root/Target>

    def Xform "Target"
    {
    }
}
